using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeirdScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("This is the Start() event");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("This is the Update() event");
    }
}
